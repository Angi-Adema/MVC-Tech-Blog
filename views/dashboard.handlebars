{{#each posts}}
  <div>
    <a href="/dashboard/update/{{id}}"><h2>{{title}}</h2></a>
    <p>Published: {{format_date createdAt}}</p>
  </div>
{{/each}}

<a href="/dashboard/create">Create A New Post</a>